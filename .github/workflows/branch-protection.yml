name: Branch Protection Check

on:
  pull_request:
    branches: [ main ]

jobs:
  check-author:
    runs-on: ubuntu-latest
    steps:
      - name: Check if author is allowed
        run: |
          echo "PR Author: ${{ github.actor }}"
          echo "Verificando permissões..."
          # Este workflow serve como um checkpoint adicional
          # Configure as branch protection rules no GitHub para:
          # 1. Require pull request before merging
          # 2. Require approvals (1)
          # 3. Restrict who can push to matching branches
